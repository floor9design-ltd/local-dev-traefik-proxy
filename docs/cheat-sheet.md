# Cheat Sheet for a Laravel install

## Set the permissions/ownership:

* `sudo chmod -R 775 storage bootstrap/cache`
* `sudo chown -R $USER:www-data storage bootstrap/cache`


## Useful commands

* `docker compose exec node npm run build`